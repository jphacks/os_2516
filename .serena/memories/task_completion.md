- iOS 変更時は `xcodebuild` でビルド/テストを実行し、重要 UI やゲームプレイ改修前にシミュレータ Tests を通す。
- Go 変更時は `go test ./...` を実行し、WebSocket/攻撃判定ロジックのテーブル駆動テストを整備する。
- ドキュメント更新(設計変更・要件変更・タスク進捗)を `design.md`/`requirements.md`/`tasks.md` に記録する。
- 秘密情報は `.env.example` 等で共有し、本番シークレットはコミットしない。